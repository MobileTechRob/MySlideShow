<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MySlideShow.MainPage">


    <ContentPage.Resources>
        <DataTemplate x:Key="evenTemplate">
            <Grid BackgroundColor="LightGray">
                <!-- Content for even rows -->
                <Label Text="{Binding YourProperty}" />
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="unevenTemplate">
            <Grid BackgroundColor="White">
                <!-- Content for uneven rows -->
                <Label Text="{Binding YourProperty}" />
            </Grid>
        </DataTemplate>
    </ContentPage.Resources>

    <Grid x:Name="CollectionLayout" Grid.Row="0">
        <Grid.RowDefinitions>
            <RowDefinition x:Name="Pics" Height="*"/>
            <RowDefinition x:Name="Button" Height="50"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <CollectionView x:Name="PictureCollection" SelectionMode="Single" SelectionChangedCommand="{Binding SelectedPictureCommand}" SelectionChangedCommandParameter="{Binding SelectedItem, Source={x:Reference PictureCollection}}" ItemsSource="{Binding ListOfPictures}" VerticalScrollBarVisibility="Always" Grid.Row="0" Grid.ColumnSpan="2">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition>
                            </RowDefinition>
                            <RowDefinition>
                            </RowDefinition>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Image Source="{Binding FilePath}" HeightRequest="100" WidthRequest="100" Aspect="AspectFit" Grid.Column="0" Grid.RowSpan="2"/>
                        <Label Text="  Display Duration (secs): " Grid.Row="0" Grid.Column="1" HorizontalTextAlignment="Center" VerticalTextAlignment="Center"/>
                        <Label Text="{Binding DisplayDuration}" Grid.Row="0" Grid.Column="2" HorizontalTextAlignment="End" VerticalTextAlignment="Center"/>

                        <Label Text="  Transition Duration (secs): " Grid.Row="1" Grid.Column="1" HorizontalTextAlignment="Center" VerticalTextAlignment="Center"/>
                        <Label Text="{Binding TransitionTime}" Grid.Row="1" Grid.Column="2" HorizontalTextAlignment="End" VerticalTextAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Text="Add Picture" x:Name="AddPictureButton" Command="{Binding AddPictureCommand}" Grid.Row="1" Grid.Column="0" />
        <Button Text="Generate Show" x:Name="GeneratePictureButton" Command="{Binding GenerateShowCommand}" Grid.Row="1" Grid.Column="1" />
    </Grid>
</ContentPage>
