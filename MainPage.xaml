<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MySlideShow.MainPage">


    <Grid x:Name="GridLayout">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" Grid.Row="0" />
            <RowDefinition Height="*" Grid.Row="1" />
        </Grid.RowDefinitions>


        <Grid x:Name="CollectionLayout" Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition x:Name="Pics" Height="Auto"/>
            </Grid.RowDefinitions>

            <CollectionView x:Name="PictureCollection" SelectionMode="Single" SelectionChangedCommand="{Binding SelectedPictureCommand}" SelectionChangedCommandParameter="{Binding SelectedItem, Source={x:Reference PictureCollection}}" ItemsSource="{Binding ListOfPictures}" VerticalScrollBarVisibility="Default" Grid.Row="0">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="5" Padding="5">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="10" />
                            </Border.StrokeShape>
                            <Grid>
                                <Grid.RowDefinitions>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Image Source="{Binding FilePath}" HeightRequest="100" WidthRequest="100" Aspect="AspectFit" Grid.Column="0"/>
                                <Label Text="   Display Duration (secs): " Grid.Column="1" HorizontalTextAlignment="Center" VerticalTextAlignment="Center"/>
                                <Label Text="{Binding DisplayDuration}" Grid.Column="2" HorizontalTextAlignment="End" VerticalTextAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
        
        <Grid x:Name="ButtonLayout" Grid.Row="1" VerticalOptions="End">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Text="Add Picture" x:Name="AddPictureButton" Command="{Binding AddPictureCommand}" Grid.Row="0" Grid.Column="0" />
            <Button Text="Generate Show" x:Name="GeneratePictureButton" Command="{Binding GenerateShowCommand}" Grid.Row="0" Grid.Column="1" />
        </Grid>
    </Grid>
</ContentPage>
